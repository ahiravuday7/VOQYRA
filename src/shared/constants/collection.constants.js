/*
|--------------------------------------------------------------------------
| Collection Statuses
|--------------------------------------------------------------------------
*/

export const COLLECTION_STATUSES = Object.freeze({
  ACTIVE: "active",
  INACTIVE: "inactive",
});

export const COLLECTION_STATUS_VALUES = Object.freeze(
  Object.values(COLLECTION_STATUSES),
);
