/*
| Address Types
*/

export const ADDRESS_TYPES = Object.freeze({
  HOME: "home",
  WORK: "work",
  OTHER: "other",
});

export const ADDRESS_TYPE_VALUES = Object.freeze(Object.values(ADDRESS_TYPES));
