/*
|--------------------------------------------------------------------------
| Brand Statuses
|--------------------------------------------------------------------------
*/

export const BRAND_STATUSES = Object.freeze({
  ACTIVE: "active",
  INACTIVE: "inactive",
});

export const BRAND_STATUS_VALUES = Object.freeze(Object.values(BRAND_STATUSES));
